# CS198Project2
Natural language to SQL interface using a local LLM (≤4B params). Converts user questions into SQL queries, executes them via SSH on a PostgreSQL database, and returns formatted results.
